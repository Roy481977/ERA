# The WebAssembly module is built by CI (GitHub Actions) on every push — not
# committed. See ../.github/workflows/pages.yml.
pkg/
# locally vendored three for headless plate renders — not shipped
vendor/
world.json

# generated blockout inputs (derived from design/bible + sim)
world-coords.json
world.json
