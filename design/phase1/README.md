# Phase 1–4 stream (2026-07-26/27) — index

The visual-language discovery (Sheets 01–06), District 01 (board, built
render, Build v1 level data), the walk studies, and the first simulation
layer. Written canon lives in the claude.ai ERA project under `era/design/`
and `era/creative-decisions/`; this repo holds the code and the boards.

- `design/boards/` — final sheets and boards (SVG source + PNG):
  era-lang-v2 (architectural language) · era-style-v2 (the voice) ·
  era-love-v2 (lovability) · era-street-v2 (streets) · era-place-v2
  (places) · era-walk-v2 (the walk) · era-district-01 (plan+views) ·
  era-composition (nine town topologies) · era-district01-built (one
  worldspace) · era-walking-the-town (20 frames) · era-walking-in /
  era-cine (perspective cinematography) · era-build-v1 (production package).
- `tools/artstudio/` — the drawing studio: svgkit (ortho + perspective
  cameras, painter renderer), era_lang (materials/openings), charm
  (parametric hero), lovable/streets/places/walk (vocabulary), district01
  (the built worldspace), make_* (board composers).
- `tools/sandbox/` — `district_level.py` (THE LEVEL — data, drawing derived),
  `sandbox.py` (smallest deterministic sim), `story_metrics.py` (the
  researcher layer), `sim_resident.py` (first-resident precursor), and the
  seed-27 reports.

Decisions: CD-024 (PROPOSED, held), CD-025, CD-026, CD-027 (**RATIFIED**),
CD-028 — see `creative-decisions/`.
