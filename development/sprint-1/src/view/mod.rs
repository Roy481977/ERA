//! View support: the spatial layout the engine owns so that snapshots can carry
//! ready-to-draw geometry. Rendering is driven from state; the state includes
//! where things are on the map.

pub mod layout;
